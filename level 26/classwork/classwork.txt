1. ახსენით რას ეწოდება Indexing

Indexing პითონში გამოიყენება სიებთან (არა მარტო, ცვლადში შენახული string ტიპის ელემენტის ასოებსაც აქვს ინდექსი). 
ანუ, სიის ყველა ელემენტს აქვს საკუთარი ინდექსი, რომელიც იწყება 0-დან. ინდექსების მეშვეობით შეგვიძლია 
slicing (სიიდან კონკრეტული ელემენტის გამოძახება) და სხვა "აქტივობები"